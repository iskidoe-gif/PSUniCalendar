<div class="mb-6">
    <div>
        <h1 class="text-3xl font-semibold text-slate-900">{{ $title }}</h1>
        @isset($subtitle)
            <p class="mt-2 text-slate-500">{{ $subtitle }}</p>
        @endisset
    </div>
</div>
